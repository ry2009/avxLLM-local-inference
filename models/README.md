# Models Folder
Place downloaded base models, adapters, or converted GGUF weights here. Files in this directory are intentionally gitignored—use `models/` as a scratch space for local experiments and do not commit proprietary weights.
